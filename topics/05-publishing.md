# 05 公開の自動化

## 現状の結論

- 公開先: itch.io、DLsite、BOOTH、pixiv、note(参考: https://note.com/kakasi4423/n/n75ec8e897da7)、X。Steam / App Store / Google Play は時々。家庭用機は後回し。
- 日英の説明文・一言コメントまで自動で作る。

## 検討中の論点

1. **自動化の可否(現時点の見立て)**
   | 公開先 | 手段 | 見立て |
   |---|---|---|
   | GitHub Pages | Actions | 完全自動 |
   | itch.io | 公式 CLI butler | 完全自動 |
   | note | 公式 API なし | 記事本文を生成し、人間が貼る |
   | X | API 有料 | 投稿文生成 + 手動、または低額 API |
   | BOOTH / DLsite / pixiv | API なし | パッケージ・説明文・画像を全部用意し、人間がアップロードのみ |
   | Steam | Steamworks SDK / steamcmd | 自動化可だが後回し |
2. 「公開パッケージ」の標準形を決める(zip 構成、スクショ枚数とサイズ、アイコン、説明文の長さ別バリエーション)
3. 公開の承認は誰がどこで押すか(GitHub の手動トリガー、スマホから)
4. 上記 note 記事の内容を研究に取り込む

## itch.io 公開の実地(2026-09-14、tetrishoot → mukkii.itch.io/galaxtiris)

- 作品 repo に `publish-itch.yml` を 1 本足した(main push と手動実行で butler が dist を html5 チャンネルへ push)。ai-dev-infra 版から認可ゲートを外した最軽量版。雛形にこのまま入れる。
- 人間の手間の切り分け:
  | 作業 | 今後 |
  |---|---|
  | itch.io の作品ページ新規作成(API なし) | **毎作 1 回、人間**。入力内容は docs/PUBLISH.md から AI が用意し貼るだけ |
  | Metadata タブの Languages と AI 生成の申告 | 同上(ページ保存後に出る) |
  | BUTLER_API_KEY を secret に入れる | **不要にする**。アカウントで 1 つ。組織 secret か雛形作成時に自動設定 |
  | ワークフロー追加 | 不要。雛形に同梱 |
- itch.io の設定メモ(HTML ゲーム): Embed in page、Viewport はゲームの論理解像度(縦長 540×720 等)、Mobile friendly / Fullscreen / Auto start オン、Draft で保存 → butler push → Public。

## ホスティング: Cloudflare Pages の評価(2026-09-15)

結論: **採用する。ただし GitHub Pages を置き換えるのではなく、条件付きで併用する。**

| | GitHub Pages | Cloudflare Pages |
|---|---|---|
| private repo から公開サイト | **不可**(Pro が要る) | **可**(無料のまま) |
| 転送量 | ソフト上限あり | 実質無制限 |
| ブランチごとのプレビュー URL | なし | **あり**(複数案の比較に効く) |
| サーバー処理 | 不可 | **Workers**(無料枠 10 万リクエスト/日) |
| 保存 | なし | KV / D1(SQLite)/ R2(大容量、下り無料) |
| 設定の手間 | ゼロ(もう動いている) | アカウント + API トークンを 1 回 |

**GitHub Pages を private にすると見れなくなる問題は、Cloudflare Pages で解決する。** repo が private でもサイトは公開できる。

### サーバー処理(Workers)で開く道
- オンラインランキング、セーブの同期、簡単なマルチプレイの仲介。
- AI の API キーを隠して呼ぶ中継(ゲーム内で LLM を使う作品が作れる)。
- アクセス解析、コメント、いいね。
- R2 は **大きな素材の置き場**として Drive の代わりになる(下り転送が無料なので、ゲームから直接読める)。

### 採用の形(雛形への入れ方)
- 既定は GitHub Pages のまま(ゼロ設定で既に動いている)。
- 雛形に `deploy-cloudflare.yml` を **1 本追加**し、secret `CLOUDFLARE_API_TOKEN` が設定されている時だけ動くようにする。無ければ黙って飛ばす。
- ビルドは GitHub Actions で行い `wrangler pages deploy dist` で送る(Cloudflare 側の管理画面に設定を持たせない = 設定は全部 repo にある)。
- 使い分け: パイロット = GitHub Pages / **private にしたい作品・製品化・サーバー処理が要る作品 = Cloudflare**。
- 同種の選択肢として Netlify もある(このセッションに MCP 接続済み)。Workers 相当が要らないなら Netlify でもよいが、**無料枠と R2 の強さで Cloudflare を推す**。

### 参考にしたい記事(未読、要取り込み)
- https://blog.toripota.com/entry/prepota (個人開発のブラウザゲーム。「アツマールが忘れられない」ほか)
  - 会議室の実行環境からは egress 制限で読めない。**07 の収集レイヤー(GitHub Actions)からなら読める**ので、そちらで取得して知見カード化する。

## セカンドオピニオン(GPT / Gemini)を受けた方針の確定(2026-09-18)

ユーザーが同じ事実を GPT と Gemini に提示して意見を求めた。**両者は独立に同じ結論に収束し、会議室(Claude)の見立てを 3 点で修正した。** 修正を採用する。

### 会議室が間違えていた点

1. **原因を IP に寄せすぎた。** 4 つの失敗は種類が違う(403 / 認証中の制限 / 追加認証)。IP は有力な要因だが、これだけでは断定できない。実際 GitHub Actions では「データセンター IP」と「自動操作ブラウザ」が常に同時に成立していて、切り分けられていない。
2. **自動操作ブラウザ自体が検知される。** TLS / HTTP2 のフィンガープリント(JA3/JA4)と CDP の痕跡は、**IP を変えても残る**。住宅回線に移しても Cloudflare 系は弾く可能性がある。Cloudflare は Playwright での本番チャレンジ解決を対象外と明言している。
3. **X のブラウザ自動化は「グレー」ではなく明確に規約違反。** X の自動化ルールは API を使わない自動操作を禁じている。凍結リスクを考えると、投資する理由がない。会議室は「ユーザー判断」として扱っていたが、事実として禁止と伝えるべきだった。

### 確定した方針

**基本は半自動。X は公式経路。機器の購入は最後。**

| 公開先 | 方式 |
|---|---|
| itch.io | **butler で自動**(唯一成功。継続) |
| Pages / Cloudflare | Actions で自動 |
| X | **公式 API**、または **itch.io の RSS → Buffer 等の OAuth 連携**(後者なら費用ゼロ・規約内) |
| PLiCy / ふりーむ / Newgrounds | **半自動**。素材と文面は全自動生成、投稿だけ人間 |

### 却下・降格したもの

- 日本の VPS: 却下。さくら・ConoHa も WAF からはホスティング IP。Azure と判定は変わらない。
- VM + 住宅用プロキシ: 却下。費用の割に TLS/CDP の検知は防げない。
- 自宅 self-hosted runner: **降格**(「本命」→「検証後の候補」)。Raspberry Pi は ARM64 で Playwright の依存関係が面倒。置くなら Intel N100 のミニ PC。

### 採用する新しい案(会議室が挙げていなかった)

1. **「スマホ用の公開キット」**(GPT 案)。Actions が各サイト用の ZIP・画像・日英の文面・投稿画面へのリンク・コピーボタン・サイト別の投稿状況(未 / 済 / 公開確認済)を 1 ページにまとめる。半自動でも「毎回素材を探して文を整える」時間が消える。**これを次に作る。**
2. **Tampermonkey 等のユーザースクリプト**(Gemini 案)。普段使いのブラウザで投稿画面を開いた時だけ、GitHub Releases から最新 ZIP と文面を取って入力欄に流し込む。**本物のブラウザ・本物の IP・本物のセッション**なので検知の問題が原理的に起きない。3 サイトの upgrade 経路。
3. **itch.io の RSS → 外部連携で X 告知**(Gemini 案)。itch.io を更新すると RSS が更新される。Buffer や Make に読ませて OAuth で X に投稿。**X API の従量課金も、規約違反のスクレイピングも不要。** 最初に試す価値が高い。
4. **運用の分離**(GPT 案)。新規登録と更新を分ける。アップロード成功と公開完了を分ける(公開確認前に X へ告知しない)。サイト別に再開できるようにする(二重投稿を防ぐ)。開発中の更新先と正式リリース先を分ける。

### 費用の訂正

X 公式 API は定額ではない。**URL 付き投稿 1 件 $0.20**。月 4 件なら約 120 円、10 件で約 300 円、30 件で約 900 円。会議室が「月 900 円」と一括りにしたのは、1 日 1 本の想定に引きずられた不正確な言い方だった。少数の告知なら遥かに安い。

### まだ確かめていないこと(GPT 案の実験)

自宅回線で **「普通のブラウザの手動操作」と「Playwright」を比較する**。これで IP と自動操作を切り分けられる。
- 手動○ / Playwright× → IP だけでは解決しない。ユーザースクリプト案が正解
- 両方○ → 実行環境を移す価値あり
- 両方× → アカウント状態やサイト側の制限を疑う

## 実測の結論: クラウドからのブラウザ自動投稿は成立しない(2026-09-18)

`dev-tools` で実際に試した結果。**推測ではなく実測。**

| 公開先 | 手段 | 結果 |
|---|---|---|
| GitHub Pages / Cloudflare | Actions | ○ 稼働 |
| itch.io | **公式 CLI(butler)** | ○ 稼働 |
| PLiCy | ブラウザ自動操作 | **× ログイン画面の手前で Cloudflare が 403。入力欄 0 個** |
| X | ブラウザ自動操作 | **× ユーザー名を送った時点で「ログインを一時的に制限しました」** |
| Newgrounds | ブラウザ自動操作 | **× ログイン画面にボット判定。送信すると確認画面(認証コード)** |
| ふりーむ! | ブラウザ自動操作 | 確認中 |

### そこから得た法則(知見に昇格させる)

> **公式 API があるものだけが自動化できる。ブラウザ自動操作は、大手サイトではクラウドからまず通らない。**

原因は共通していて、GitHub のランナーが**データセンター IP** だから。住宅回線とは扱いが違う。
パスワードの有無、cookie の有無、人間らしい振る舞いの作り込み、どれも関係なかった。**ログイン画面に届く前、あるいは認証情報を送った瞬間に弾かれる。**

### 「手がない」わけではない。塞がっているのはクラウドだけ(2026-09-18 追記)

ユーザーの指摘「世の中で効率化しているところはあるはず」は正しい。**行き詰まりの原因は道具ではなく、通信の出どころ。**

- Python でも Selenium でも、ローカル LLM を足しても **何も変わらない**。サイトが見ているのは「どの IP から来たか」であって、どのツールを使ったかではない。
- 逆に言えば、**住宅回線から出れば、いま書いたスクリプトはそのまま動く可能性が高い**。作ったものは無駄にならない。

### 解決の本命: 自宅に「自前ランナー」を置く

GitHub Actions には **self-hosted runner**(自分の機械で実行する仕組み)がある。これを自宅の機械に入れると:

- 操作はいままで通り。スマホから Actions のボタンを押すだけ。
- 実行だけが自宅の機械で起き、**住宅回線の IP** で外に出る。
- 会議室も GitHub も、いまの構成を一切変えなくてよい。

置く機械の候補:

| 候補 | 費用 | 常時稼働 | 備考 |
|---|---|---|---|
| **Raspberry Pi 5** | 本体 1.5〜2 万円、電気代 月 50〜100 円 | ○ | 小さく静か。3〜7W。**「デスクトップを付けっぱなしにしたくない」を満たす本命** |
| 古いノート PC / モバイル PC | 0 円 | ○ | 余っていれば最有力。蓋を閉じても動く設定にする |
| デスクトップ(RTX 4080) | 電気代 高い | × | 公開する時だけ起動して走らせる。常時稼働はしない |
| 住宅用プロキシ | 月 $5〜15 | ○ | 機械を置かずに済むが、別の壊れやすさが増える |

### 併せて効く手

- **セッションの使い回し**。自宅の実機ブラウザで 1 回ログインし、その状態を保存して使う。ログイン自体を繰り返さないので、確認画面に当たる機会がほぼ無くなる。
- **公式 API を優先**。itch.io は CLI、X は従量課金 API。API があるものは自前ランナーでも API を使う。

### 方針の修正

- **自動化する公開先は「公式 API があるもの」に限る。** itch.io、Pages、Cloudflare。X は従量課金の公式 API なら可能(1 投稿リンクありで約 $0.20、告知 1 回/日で月 900 円前後)。
- **API が無い公開先は半自動に落とす。** AI が ZIP・日英説明文・スクショ・動画・タグを全部揃え、人間がフォームに貼って送信する。1 サイト 5 分程度。
- **自宅に self-hosted runner を置くのが本命。** `dev-tools` のスクリプトはそのまま使える。Raspberry Pi なら常時稼働でも月 100 円以下。
- 各サイトには `blockedFromCloud` の印を付けてあり、Actions 上では最初に止まる。無駄な試行でアカウントを目立たせない。

### 副産物(残ったもの)

- `scripts/peek.mjs` と workflow「ページを覗く」。会議室から到達できないサイトの構造を Actions 経由で調べる道具。今後も使える。
- `lib/browser.mjs` の確認画面検出。突破せず止める仕組み。
- ログイン判定の教訓(下記)。

## クラウド完結の投稿自動化(2026-09-17 設計)

ユーザーが PLiCy / ふりーむ / Newgrounds / X のアカウントを取得。**家の PC を使わずクラウドで完結させたい。**

### 手段は 2 種類

| 公開先 | 手段 | クラウド可 | 資格情報 |
|---|---|---|---|
| itch.io | 公式 CLI(butler)| ○ 既に稼働 | API キー(secret) |
| GitHub Pages / Cloudflare | Actions | ○ | トークン |
| **X(Twitter)** | **公式 API** | ○ | API キー 4 種(secret)。無料枠で 1 日 1 本の告知は十分 |
| PLiCy / ふりーむ / Newgrounds | Playwright(ヘッドレス)| ○ GitHub Actions 上で動く | **ログイン済みセッション cookie**(secret) |

ブラウザ自動操作も **GitHub Actions のランナー上で動く**。自宅 PC は要らない。

### 認証の扱い(ここが要)

- cookie は **repo の Actions secret** に暗号化して置く。fb-radio-auto の失敗(cookie を repo にコミット)を繰り返さない。secret はコードに入らない。
- cookie の取り出しは **一度だけデスクトップのブラウザが要る**(スマホからは取り出せない)。PC を常時起動する必要はなく、初回と失効時の数分だけ。
- 代替案(パスワードを secret に置いて毎回ログイン)は、2 段階認証と CAPTCHA で壊れやすく、パスワードを持つ分だけ危険。**採らない。**

### 正直な риск

1. **cookie は失効する**(数週間〜数か月)。失効を検出したら人間に知らせ、投稿は止める(黙って失敗させない)。
2. **データセンター IP が弾かれることがある**。GitHub のランナーは自宅とは別 IP。弾かれた場合の逃げ道: 自宅 PC で同じスクリプトを走らせる / 住宅用プロキシ(有料)。まず試して確かめる。
3. 規約。ユーザー判断で「まず入れて、何か言われたら考える」(2026-09-15)。

### 進め方(2026-09-17 着手)

会議室の実行環境からは **4 サイトとも egress 制限で到達できない**(接続 000)。画面を見ずにセレクタは書けないので 2 段階にした。

1. **第 1 段階(完了)**: `dev-tools` に共通層 + `scripts/explore.mjs` + workflow「画面構造を調べる」を作成。Actions 上でログインし、投稿画面のスクリーンショットと入力欄一覧を成果物として持ち帰る。投稿はしない。
2. **第 2 段階**: 持ち帰った構造を見て、サイトごとの投稿処理を書く。

X は従量課金(1 投稿リンクありで約 $0.20、月 900 円前後)を避けてブラウザ自動操作にする、というユーザー判断(2026-09-17)。ただし **GitHub ランナーはデータセンター IP のため、X の初回ログインで確認画面が出る可能性が高い**。出たら自宅で 1 回ログインしてセッションを持ち込む形に切り替える。

### 置き場

- 資格情報を持つ自動化なので **private repo** に置く。→ 2026-09-17 `mukkii-game/dev-tools` を新設し、そこに実装。
- `ai-ops` には置かない。あちらは repo 管理用の強いトークンを持つので、役割を混ぜない。

### X の告知文
- `docs/PUBLISH.md` と RELEASE-KIT の投稿文案をそのまま使う。画像 2 枚(シューティング + ボス)と動画を添付。
- 公開直後、1 日後、1 週間後で文面を変えて 3 回投げる案。まずは 1 回から。

## 同人 Web ゲーム公開場所の広げ方(2026-09-15、要規約再確認)

- 複数投稿は原則可(itch.io / Newgrounds / GameJolt / PLiCy / ふりーむ は非独占)。独占条項はコンテストや一部パブリッシャー系(CrazyGames, Poki 等)のみ。
- 2 段階運用: **全作品 → Pages + itch.io(自動)**。1 週間の反応を見て **手応えのあるものだけ → Newgrounds(英語圏)+ PLiCy か ふりーむ(日本語圏)**(半自動)。
- **2026-09-15 ユーザー判断: Newgrounds と PLiCy はブラウザ自動操作で投稿まで自動化する。** 明示的な禁止が見当たらないこと、審査に日数がかかることから、まず入れて問題が出たら考える方針。会議室の留意点(実行前に把握しておく): アカウント停止のリスクはゼロではない、UI 変更で壊れる、実行には PC が要る、ログイン情報の扱い。→ 対策として「投稿用の専用 Chrome プロファイルを人間が一度ログインしておき、スクリプトはそのプロファイルを使う(パスワードをスクリプトに持たせない)」「1 日の投稿数を絞る」「失敗したらリトライせず人間に報告」を設計に入れる。
- YouTube Playables は招待申し込み中。通ったら対象に追加。
- 場所別: Newgrounds = 自然流入最強、メダル / スコア API あり(組み込み可)。GameJolt = 同系小規模。PLiCy = 日本 HTML5、収益分配。ふりーむ = 審査あり日数要。夢現 = DL 型中心で効果薄。unityroom = Unity WebGL 専用(Godot 不可)。YouTube Playables = 招待制、対象外。
- dev-tools 候補:
  - `package_for.py <site>`: 各サイトの要件(zip 構成、画像サイズ、文字数)に合わせた一式を `tools/out/<site>/` に出力。
  - `post_newgrounds.mjs` / `post_plicy.mjs`: Playwright の persistent context(専用プロファイル)でフォームを埋めて送信。入力は `docs/PUBLISH.md` と `tools/out/<site>/`。結果 URL を標準出力に返す。失敗時はスクショを残して停止。
  - 実行場所は自宅 PC(ログイン済みプロファイルが要るため)。GitHub Actions からは動かさない。

## 宣伝動画の自動生成(ユーザー要望 2026-09-13)

- できる。流れ: 自動プレイ(既存の cedec-fable `tools/autoplay.mjs` や panzer-tokoron の soak ツールが元)→ Playwright の録画機能で動画取得 → ffmpeg でタイトル・字幕(日英)・BGM(CC0)を合成 → 15 秒 / 60 秒の 2 種類を出力 → itch.io のページ、X、YouTube(API で投稿可)に配る。
- 必要なもの: 作品側に「自動プレイ用の入力スクリプト or デモモード」を雛形の作法として持つ(AGENTS.md に「`?auto=1` でデモが走る」を既定にする)。
- 映像生成 AI は使わない(06 の方針)。録画 + 編集の定型処理だけで作る。

## 調べたこと

- 2026-09-13: Pages と itch.io の自動公開は ai-dev-infra の deploy-pages.yml / publish-itch.yml で実証済み(Yasu)。ユーザー評価「楽だった、踏襲」。上の表の 2 行は「実装済み」。

- (未着手)

## 次にやること

- [ ] note 記事を読んで要点を research/ に保存
- [ ] 公開パッケージ標準形の案を書く
